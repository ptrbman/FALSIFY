void stack_fact_push_and_pop_returns_same() {
    int ul = _;
    #ASSUME 0<=ul
    #ASSUME ul<STACK_SIZE
    for (int i = 0; i<ul; i++)
        push(0);
    int val = _;
    push(val);
    int real;
    int retval = pop(&real);
    #FACT real == val
}
