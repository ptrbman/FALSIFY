void stack_fact_push_and_pop_returns_same() {
    int val = _;
    int ul = _;
    for (int i = 0; i<ul; i++)
       push(0);

    push(val);

    int real;
    pop(&real);
    int expected = val;

    #FACT real == expected
}
