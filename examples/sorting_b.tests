#include "sorting.h"
#include <stdlib.h>

#BEGINTEST
void sorting_test_1() {
  int count = _;
  #ASSUME count == 3

  int *A = malloc(sizeof(int)*count);
  for (int i=0; i<count; i++)
    A[i] = _;

  bubble_sort(A, count);

  int err_i = _;
  int err_j = _;
  #ASSUME err_i < err_j
  #ASSERT A[err_i] <= A[err_j]
  //for (int i=1; i<count; i++)
    //#ASSERT A[i] >= A[i-1] 
}
#ENDTEST
